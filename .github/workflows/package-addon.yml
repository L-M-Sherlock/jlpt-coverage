name: Package Artifacts

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  package:
    name: Build add-on and Yomitan dictionary
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Sync project
        run: uv sync --locked

      - name: Compile Python sources
        run: |
          uv run python -m py_compile \
            jlpt_coverage/*.py \
            scripts/*.py \
            anki_addon/jlpt_coverage/__init__.py \
            anki_addon/jlpt_coverage/i18n.py

      - name: Package add-on
        run: uv run scripts/package_anki_addon.py

      - name: Validate add-on archive
        run: uv run scripts/validate_anki_addon.py

      - name: Package Yomitan dictionary
        run: uv run scripts/build_yomitan_jlpt_dict.py

      - name: Validate Yomitan dictionary
        run: uv run scripts/validate_yomitan_jlpt_dict.py

      - name: Upload add-on artifact
        uses: actions/upload-artifact@v4
        with:
          name: jlpt_coverage-ankiaddon
          path: dist/jlpt_coverage.ankiaddon
          if-no-files-found: error

      - name: Upload Yomitan dictionary artifact
        uses: actions/upload-artifact@v4
        with:
          name: eggrolls-jlpt-yomitan
          path: dist/eggrolls-jlpt-yomitan.zip
          if-no-files-found: error

      - name: Upload direct assets to release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_NAME: ${{ github.ref_name }}
        run: |
          assets="dist/jlpt_coverage.ankiaddon dist/eggrolls-jlpt-yomitan.zip"
          if gh release view "$TAG_NAME" >/dev/null 2>&1; then
            gh release upload "$TAG_NAME" $assets --clobber
          else
            gh release create "$TAG_NAME" $assets \
              --title "$TAG_NAME" \
              --notes "Automated JLPT Coverage artifact build."
          fi
